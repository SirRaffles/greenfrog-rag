# FastAPI and Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Data Validation
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP Client
httpx>=0.27.0  # Updated for ChromaDB 0.5.23 compatibility
requests==2.31.0

# WebSocket
python-socketio==5.10.0
websockets==12.0

# Background Tasks
celery==5.3.4
redis==5.0.1

# Database
sqlalchemy==2.0.23
alembic==1.13.0
chromadb==0.5.20  # ChromaDB Python client - compatible with latest server
numpy<2.0.0  # Pin NumPy to < 2.0 for sentence-transformers compatibility

# Utilities
python-dotenv==1.0.0
python-dateutil==2.8.2
aiofiles==23.2.1

# Logging
structlog==23.2.0

# RAG Enhancements
rank-bm25==0.2.2          # BM25 keyword search for hybrid retrieval
# flashrank==0.2.5          # CPU-optimized reranking (requires C++ build tools - will add back with multi-stage build)
sentence-transformers>=3.0.0  # For embeddings generation (updated for huggingface-hub compatibility)
prometheus-client==0.19.0  # Metrics collection and monitoring
